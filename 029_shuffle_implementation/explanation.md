# Problem 29: Implement Shuffle (Without Built‑in Functions)

## Problem
Write a function that takes a list (or any sequence) and returns a **shuffled** (randomly permuted) copy of it, without using Python's built‑in `random.shuffle()` or `random.sample()`. The result should be a new list containing all the original elements in a random order.

---
