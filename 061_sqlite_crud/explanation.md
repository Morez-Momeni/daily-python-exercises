# Problem 61: SQLite CRUD Operations

## Problem
Build a small SQLite program that manages a `users` table with four operations: Insert, Fetch, Delete, and Update.

## My Solution

I used Python's built-in `sqlite3` module. The table is created once at startup, then four functions wrap the four CRUD operations.